{
  "name": "edge-openlist",
  "version": "0.1.0",
  "private": true,
  "description": "OpenList 风格的网盘聚合服务，运行于 Cloudflare Worker（Hono + D1（强制）+ KV + R2（可选）+ Vue3）",
  "workspaces": [
    "worker",
    "web"
  ],
  "scripts": {
    "dev:worker": "wrangler dev --config wrangler.dev.toml",
    "build": "npm run build:web",
    "build:web": "npm --workspace web run build",
    "db:migrate": "wrangler d1 migrations apply DB --remote",
    "deploy": "wrangler deploy",
    "typecheck": "npm --workspace worker run typecheck",
    "dev:web": "npm --workspace web run dev",
    "test": "tsx test/drivers.test.ts && tsx test/crypt.test.ts && tsx test/route_smoke.ts"
  },
  "cloudflare": {
    "bindings": {
      "JWT_SECRET": {
        "description": "JWT 签名密钥。部署后请到 Cloudflare 控制台 Workers 变量页改成 `openssl rand -hex 32` 生成的随机值。"
      }
    }
  },
  "devDependencies": {
    "tsx": "^4.23.12"
  }
}
